<Proxy "fcgi://localhost/" enablereuse=on max=10 timeout=60 connectiontimeout=5>
</Proxy>

<FilesMatch "\.php$">
    <If "-f %{REQUEST_FILENAME}">
        SetHandler "proxy:unix:/var/run/php-fpm.sock|fcgi://localhost/"
    </If>
</FilesMatch>

DirectoryIndex index.php index.html